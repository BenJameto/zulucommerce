apiVersion: v1
kind: Service
metadata:
  name: product-service
  labels:
    app: product-service
    component: product
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
  - name: http
    port: 3002
    targetPort: 3002 